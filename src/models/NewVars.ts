// Objective: Create a class to store the new variables that will be used in the application.

class NewVars {
  private zRow: number[];

  constructor(zRow: number[]) {
    this.zRow = zRow;
  }

  calculateReducedCost() {
    return 0;
  }
}
